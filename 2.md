graph TD
    %% Entities
    User[User] -->|Access| App[Health App]
    App -->|Request| API[Backend API]
    API -->|Query| DB[Database]
    App -->|Authenticate| Auth[Authentication Service]
    App -->|Payment| PaymentGateway[Payment Gateway]
    App -->|Data Transfer| ExternalService[External Health Data Service]
    API -->|Store| Log[Logging Service]

    %% Data Flows
    User -- "Login Credentials" --> Auth
    Auth -- "Session Token" --> App
    App -- "Health Data, Payments" --> API
    API -- "User Info, Medical Data" --> DB
    PaymentGateway -- "Transaction Details" --> API
    ExternalService -- "Medical History" --> App
    App -- "Logs, Activity" --> Log
